services:
  - type: web
    name: pt-career
    env: node
    plan: free
    buildCommand: pnpm install && pnpm run build
    startCommand: node dist/index.js
    envVars:
      - key: DATABASE_URL
        scope: build
      - key: NODE_ENV
        value: production
